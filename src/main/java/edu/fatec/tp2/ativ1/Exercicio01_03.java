/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package edu.fatec.tp2.ativ1;

/**
 *
 * @author Silvio Junior
 */
public class Exercicio01_03 {
    /**
     *
     * 1. Uma variável é um nome dado a um local de armazenamento na memória do computador. 
     * Ela é usada para guardar um valor, que pode ser alterado durante a execução do programa. 
     * Cada variável tem um tipo associado, que determina o tamanho e o layout do espaço de memória 
     * da variável, a faixa de valores que pode ser armazenada nesse espaço de memória e o conjunto de
     * operações que podem ser aplicadas à variável.
     * A declaração de uma variável em Java é feita da seguinte maneira:
     * tipo nomeDaVariavel;
     * Em relação à memória, quando uma variável é declarada, o sistema reserva um espaço na memória 
     * do computador para armazenar o valor dessa variável. O tamanho desse espaço depende do tipo da variável. 
     * Por exemplo, uma variável do tipo int em Java normalmente requer 4 bytes de memória. 
     * A variável serve como uma referência a esse local de memória, permitindo que o programa leia o 
     * valor armazenado ou altere esse valor.
     * 
     * 2. Em uma linguagem compilada, o código fonte é traduzido para uma linguagem de máquina específica por 
     * um compilador antes de ser executado. O compilador gera um arquivo binário que pode ser executado diretamente
     * pelo sistema operacional.
     * Já uma linguagem interpretada, o código fonte não é traduzido para a linguagem de máquina de uma vez. 
     * Em vez disso, um interpretador lê e executa o código linha por linha enquanto o programa está sendo executado. 
     * Isso significa que o código fonte deve ser interpretado cada vez que o programa é executado.
     * A linguagem Java combina aspectos de linguagens compiladas e interpretadas. O código Java é primeiro compilado 
     * em um formato intermediário conhecido como bytecode pelo compilador Java (javac). Este bytecode é então interpretado 
     * e executado pela Máquina Virtual Java (JVM). Portanto, Java é considerado como sendo compilado e interpretado. 
     * A vantagem desse modelo é que o bytecode Java é portátil e pode ser executado em qualquer dispositivo que tenha uma JVM.
     * 
     * 3. Uma classe é um modelo que define os atributos (dados) e comportamentos (métodos) que serão encapsulados e ela é 
     * utilizada para criar objetos específicos. Já um objeto, por outro lado, é uma instância de uma classe. Quando uma classe 
     * é definida, nenhum espaço de memória é alocado. No entanto, quando ela é instanciada, ou seja, quando um objeto é criado, 
     * o espaço de memória é alocado.
     * 
     */
}
